node_modules/
dist/
logs/
*.log

# Tailwind build output (public/style.css is the committed source)
public/dist.css

# Playwright outputs (snapshot baselines under tests/e2e/**-snapshots/ ARE committed)
playwright-report/
test-results/
blob-report/

# Env files (real secrets live here; .env.example is committed instead)
.env
.env.local
.env.*.local
